import{useState}from"react";import{Link,useNavigate}from"react-router-dom";import{useAuth}from"../context/Auth.jsx";
export function Login(){const{login}=useAuth(),nav=useNavigate(),[f,setF]=useState({email:"",password:""}),[e,setE]=useState("");
return <Form title="Login" f={f} setF={setF} error={e} submit={async()=>{try{await login(f);nav("/jobs")}catch(x){setE(x.response?.data?.message||"Login failed")}}} footer={<Link to="/register">Create account</Link>}/> }
export function Register(){const{register}=useAuth(),nav=useNavigate(),[f,setF]=useState({name:"",email:"",password:"",role:"seeker",company:""}),[e,setE]=useState("");
return <Form title="Create account" f={f} setF={setF} error={e} submit={async()=>{try{await register(f);nav("/jobs")}catch(x){setE(x.response?.data?.message||"Registration failed")}}} footer={<Link to="/login">Already registered?</Link>}/>}
function Form({title,f,setF,error,submit,footer}){return <main className="auth"><form className="panel" onSubmit={e=>{e.preventDefault();submit()}}><h1>{title}</h1>{["name","email","password"].filter(k=>k in f).map(k=><input key={k} required type={k==="password"?"password":k==="email"?"email":"text"} placeholder={k} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}{"role"in f&&<select value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="seeker">Job Seeker</option><option value="employer">Employer</option></select>}{f.role==="employer"&&<input placeholder="Company name" value={f.company} onChange={e=>setF({...f,company:e.target.value})}/>} {error&&<p className="error">{error}</p>}<button className="btn"> {title}</button>{footer}</form></main>}
